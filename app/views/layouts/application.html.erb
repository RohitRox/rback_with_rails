<!DOCTYPE html>
<html>
<head>
  <title>RbackWithRails</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div class="wrap">
		<h2>
			<% if user_signed_in?  %>		
				<%= current_user.email%></h2>
				<small><b>Roles:</b> <%= current_user.roles.map(&:name) %></small>
			<% end %>
		<p class="notice"><%= notice %></p>
		<p class="alert"><%= alert %></p>
		<%= yield %>
	</div>
</body>
</html>
